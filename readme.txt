If project must be run, open NOSQLFINAL folder in VS Code or any other editor

1. Run `npm install` command in project terminal so that all dependencies will be installed
2. Run `node server.js` in project terminal
3. After you can see "Server is running on port 8000, Database connected succesfully, open `http://localhost:8000/` in your browser."


1. Mostly logic is implemented in controller/controller.js
2. All user request are hndled in app.js

GOOD LUCK
$TIMESPENT:{8627}